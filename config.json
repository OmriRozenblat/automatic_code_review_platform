{
  "max_parallel_scans": 5,
  "scan_ttl_hours": 24,

  "model": {
    "provider": "ollama",
    "name": "qwen2.5-coder:3b",
  },

  "prompts": {
    "system_prompt": "You are a strict Python code compliance classifier. Your task is only to decide whether the provided Python code complies with the provided rule. You must not rewrite code. You must not fix code. You must not suggest changes. You must not add docstrings. You must not explain. You must not use Markdown. You must not output code blocks. Your entire response must be exactly one word: YES or NO.",
    
    "user_prompt_template": "Rule:\n{rule}\n\nPython code:\n```python\n{code}\n```\n\nDoes the code comply with the rules? Answer exactly YES or NO."
  }
}
